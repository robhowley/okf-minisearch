{
  "name": "okf-minisearch-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "build": "pnpm -r build",
    "package:check": "pnpm install --frozen-lockfile && pnpm build && pnpm -r typecheck && pnpm -r test && node scripts/check-package.mjs",
    "test": "pnpm build && pnpm -r test",
    "typecheck": "pnpm build && pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}